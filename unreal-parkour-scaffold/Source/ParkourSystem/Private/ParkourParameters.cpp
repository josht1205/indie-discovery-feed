#include "ParkourParameters.h"

// Intentionally empty — UParkourParameters is a pure data container.
// Default values live in the header so the editor's "Reset to Default" works
// per-property without a custom CDO override.
